# Core scientific stack
numpy==1.24.3
scipy==1.10.1
scikit-learn==1.3.0
pynndescent==0.5.10
tqdm==4.65.0

# Utility for downloading checkpoint/features from Google Drive
gdown==5.1.0

# PyTorch is intentionally installed separately because CPU/GPU wheels differ.
#
# GPU NVIDIA L4 / CUDA 12.1:
#   python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# CPU only:
#   python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
